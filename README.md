To download dependencies, run the following command in the project directory

### `pip install -r requirements.txt`

To start, run the following command in the project directory

### `python3 server.py`

